extends ../../layout_admin.pug

block content
    h1 Edit flavour

    div#flavour-form
        include ./form.pug

    form(action="/admin/flavours/" + flavour._id + "/delete", method="post", onsubmit="return confirm('Are you sure? This cannot be undone!')")
        div.form-field
            input(type="submit", value="Remove flavour").warning
