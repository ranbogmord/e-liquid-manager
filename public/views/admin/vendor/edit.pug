extends ../../layout_admin.pug

block content
    h1 Edit vendor

    div#user-form
        include ./form.pug

    form(action="/admin/flavourvendors/" + vendor._id + "/delete", method="post", onsubmit="return confirm('Are you sure? This cannot be undone!')")
        div.form-field
            input(type="submit", value="Remove vendor").warning
