extends ../../layout_admin.pug

block content
    h1 Edit tag

    div#user-form
        include ./form.pug

    form(action="/admin/tags/" + tag._id + "/delete", method="post", onsubmit="return confirm('Are you sure? This cannot be undone!')")
        div.form-field
            input(type="submit", value="Remove tag").warning
